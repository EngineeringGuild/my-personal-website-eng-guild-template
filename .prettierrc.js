// /Users/user/Desktop/Core Guild Project/projects/colabs/eng-teams/core/personal-website/prototype/.prettierrc.js

module.exports = {
  // Printing width - line wrap at 100 characters
  printWidth: 100,
  
  // Tab width - 2 spaces
  tabWidth: 2,
  
  // Use spaces instead of tabs
  useTabs: false,
  
  // Semicolons at the end of statements
  semi: true,
  
  // Use single quotes instead of double quotes
  singleQuote: true,
  
  // Quote properties in objects only when necessary
  quoteProps: 'as-needed',
  
  // Use single quotes in JSX
  jsxSingleQuote: true,
  
  // Trailing commas where valid in ES5 (objects, arrays, etc.)
  trailingComma: 'es5',
  
  // Spaces inside object literals
  bracketSpacing: true,
  
  // Put > on the same line for JSX
  bracketSameLine: false,
  
  // Include parentheses around arrow function parameters
  arrowParens: 'avoid',
  
  // Format only files that have a pragma at the top
  requirePragma: false,
  
  // Insert pragma at the top of formatted files
  insertPragma: false,
  
  // Wrap prose if it exceeds the print width
  proseWrap: 'preserve',
  
  // HTML whitespace sensitivity
  htmlWhitespaceSensitivity: 'css',
  
  // End of line character
  endOfLine: 'lf',
  
  // Control whether Prettier formats quoted code embedded in the file
  embeddedLanguageFormatting: 'auto',
} 